import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Formula1-Race-Strategy-Sim',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
